# 🚀 Инструкция по деплою на Ubuntu VPS

## 📋 Быстрый старт

### 1. Подключение к серверу

```bash
ssh root@85.209.9.193
# или
ssh ваш_пользователь@85.209.9.193
```

### 2. Установка необходимого ПО

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
sudo usermod -aG docker $USER

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Установка Nginx
sudo apt install nginx -y

# Установка Git (если не установлен)
sudo apt install git -y
```

**Важно**: После установки Docker перезайдите в систему (`exit` и снова `ssh`).

### 3. Клонирование проекта

```bash
cd /opt
git clone <ваш-репозиторий> bst-hab
cd bst-hab
```

Или загрузите файлы через SFTP/SCP.

### 4. Настройка переменных окружения

```bash
# Создайте .env.production из примера
cp .env.example .env.production
nano .env.production
```

**Обязательно заполните**:
- `NEXT_PUBLIC_YANDEX_METRIKA_ID_KHABAROVSK` - ID счётчика для Хабаровска
- `NEXT_PUBLIC_YANDEX_METRIKA_ID_IRKUTSK` - ID счётчика для Иркутска
- Все контакты и EmailJS настройки

### 5. Деплой приложения

```bash
# Сделайте скрипт исполняемым
chmod +x deploy.sh

# Запустите деплой
./deploy.sh
```

Или вручную:

```bash
# Сборка образа
docker-compose build

# Запуск
docker-compose up -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f
```

### 6. Настройка Nginx

```bash
# Копируем конфигурацию
sudo cp nginx.conf /etc/nginx/sites-available/bst-hab

# Создаём симлинк
sudo ln -s /etc/nginx/sites-available/bst-hab /etc/nginx/sites-enabled/

# Удаляем дефолтный сайт (опционально)
sudo rm /etc/nginx/sites-enabled/default

# Проверка конфигурации
sudo nginx -t

# Перезапуск Nginx
sudo systemctl restart nginx
```

### 7. Настройка DNS

В панели управления доменами настройте:

**Для bst-hab.ru:**
```
A запись: @ -> 85.209.9.193
A запись: www -> 85.209.9.193
```

**Для irkutsk.bst-hab.ru:**
```
A запись: irkutsk -> 85.209.9.193
```

### 8. Настройка SSL (Let's Encrypt)

```bash
# Установка Certbot
sudo apt install certbot python3-certbot-nginx -y

# Получение сертификатов для обоих доменов
sudo certbot --nginx -d bst-hab.ru -d www.bst-hab.ru -d irkutsk.bst-hab.ru

# Автоматическое обновление (уже настроено)
sudo certbot renew --dry-run
```

После этого в `nginx.conf` раскомментируйте строки с редиректом на HTTPS.

---

## 🔍 Проверка работы

### Проверка основного домена

```bash
curl -H "Host: bst-hab.ru" http://localhost:3000
```

### Проверка поддомена

```bash
curl -H "Host: irkutsk.bst-hab.ru" http://localhost:3000
```

### Проверка редиректов

```bash
# Должен редиректить на поддомен
curl -I http://bst-hab.ru/irkutsk

# Должен редиректить на чистый URL
curl -I http://irkutsk.bst-hab.ru/irkutsk
```

---

## 📊 Мониторинг

### Просмотр логов

```bash
# Логи приложения
docker-compose logs -f

# Логи Nginx
sudo tail -f /var/log/nginx/bst-hab-access.log
sudo tail -f /var/log/nginx/bst-hab-error.log
```

### Мониторинг ресурсов

```bash
# Использование ресурсов Docker
docker stats

# Использование ресурсов системы
htop
# или
free -h
df -h
```

### Проверка статуса

```bash
# Статус контейнеров
docker-compose ps

# Статус Nginx
sudo systemctl status nginx

# Статус Docker
sudo systemctl status docker
```

---

## 🔄 Обновление приложения

```bash
cd /opt/bst-hab

# Получить последние изменения
git pull

# Пересобрать и перезапустить
./deploy.sh
```

Или вручную:

```bash
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

---

## 🛠️ Устранение проблем

### Приложение не запускается

```bash
# Проверка логов
docker-compose logs

# Проверка конфигурации
docker-compose config

# Пересборка с очисткой
docker-compose down
docker system prune -f
docker-compose build --no-cache
docker-compose up -d
```

### Нехватка памяти

```bash
# Проверка использования
free -h
docker stats

# Очистка неиспользуемых ресурсов
docker system prune -af --volumes
```

### Поддомен не работает

```bash
# Проверка передачи Host в Nginx
curl -H "Host: irkutsk.bst-hab.ru" http://localhost:3000

# Проверка DNS
dig irkutsk.bst-hab.ru

# Проверка конфигурации Nginx
sudo nginx -t
```

### Порт 3000 занят

```bash
# Проверка что использует порт
sudo lsof -i :3000

# Остановка старого процесса
docker-compose down
```

---

## 🔒 Безопасность

### Настройка файрвола

```bash
# Установка UFW (если не установлен)
sudo apt install ufw -y

# Разрешаем SSH
sudo ufw allow 22/tcp

# Разрешаем HTTP и HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Включаем файрвол
sudo ufw enable

# Проверка статуса
sudo ufw status
```

### Обновление системы

```bash
# Регулярно обновляйте систему
sudo apt update && sudo apt upgrade -y

# Настройте автоматические обновления безопасности
sudo apt install unattended-upgrades -y
sudo dpkg-reconfigure -plow unattended-upgrades
```

---

## 📝 Чеклист перед запуском

- [ ] Установлен Docker и Docker Compose
- [ ] Создан `.env.production` с правильными переменными
- [ ] Настроен Nginx с передачей заголовка `Host`
- [ ] Настроены DNS записи для обоих доменов
- [ ] Установлен SSL сертификат (Let's Encrypt)
- [ ] Проверена работа обоих доменов
- [ ] Настроен файрвол
- [ ] Настроен мониторинг ресурсов

---

## ✅ Готово!

После выполнения всех шагов ваше приложение будет доступно:
- 🌐 **bst-hab.ru** - основной сайт (Хабаровск)
- 🌐 **irkutsk.bst-hab.ru** - поддомен (Иркутск)

Оба домена работают в **одном Docker контейнере** с **разными счётчиками Метрики** и **максимальным SEO**! 🚀

