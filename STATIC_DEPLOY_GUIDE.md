# 🚀 Инструкция по деплою статики с SEO для двух доменов

## 📋 Что реализовано

### ✅ SEO оптимизация
- Все страницы имеют уникальные метаданные
- Schema.org разметка для обоих городов
- Sitemap.xml и robots.txt
- Canonical URLs

### ✅ Два домена
- **bst-hab.ru** - главный сайт (Хабаровск)
- **irkutsk.bst-hab.ru** - поддомен (Иркутск)

### ✅ Автоматический редирект
- При заходе на `bst-hab.ru/irkutsk` → автоматический редирект на `irkutsk.bst-hab.ru`
- Это важно для SEO и Яндекс.Директ!

---

## 🔧 Сборка проекта

```bash
npm run build
```

Это создаст папку `/out` со всеми статическими файлами.

---

## 📦 Структура после сборки

```
/out
├── index.html              # Главная Хабаровск
├── about/
│   └── index.html
├── contacts/
│   └── index.html
├── rent/
│   └── index.html
├── sale/
│   └── index.html
├── irkutsk/
│   ├── index.html          # Страница с редиректом на поддомен
│   ├── layout.html
│   └── contacts/
│       └── index.html
├── sitemap.xml
├── robots.txt
└── ...
```

---

## 🌐 Деплой на хостинг

### Вариант 1: Два отдельных проекта (рекомендуется)

#### Проект 1: Главный сайт (bst-hab.ru)
1. Создайте проект на хостинге для `bst-hab.ru`
2. Загрузите **ВСЁ содержимое** папки `/out`
3. Убедитесь, что корневой файл - `/out/index.html`

#### Проект 2: Поддомен Иркутск (irkutsk.bst-hab.ru)
1. Создайте **ОТДЕЛЬНЫЙ** проект для поддомена
2. Загрузите содержимое папки `/out/irkutsk`
3. Корневой файл - `/out/irkutsk/index.html`

**Важно:** На поддомене будет полноценный сайт с:
- Главной страницей Иркутска
- Контактами Иркутска
- Всеми разделами (аренда, продажа, о компании)
- Правильными контактами и SEO

---

### Вариант 2: Один проект + настройка веб-сервера

Если ваш хостинг поддерживает настройку nginx/Apache:

#### Nginx конфигурация:

```nginx
# Главный сайт
server {
    server_name bst-hab.ru www.bst-hab.ru;
    root /path/to/out;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Редирект /irkutsk на поддомен
    location /irkutsk {
        return 301 https://irkutsk.bst-hab.ru$request_uri;
    }
}

# Поддомен Иркутск
server {
    server_name irkutsk.bst-hab.ru;
    root /path/to/out/irkutsk;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

#### Apache (.htaccess):

```apache
# В корне главного сайта
RewriteEngine On
RewriteCond %{HTTP_HOST} ^bst-hab\.ru$ [NC]
RewriteRule ^irkutsk(.*)$ https://irkutsk.bst-hab.ru$1 [R=301,L]

# В папке /irkutsk для поддомена
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
```

---

## 🎯 Настройка Яндекс.Директ

### Для главного сайта (Хабаровск):
- **Домен:** bst-hab.ru
- **Счетчик Метрики:** отдельный для Хабаровска
- **Целевые страницы:**
  - bst-hab.ru
  - bst-hab.ru/rent
  - bst-hab.ru/contacts
  - и т.д.

### Для Иркутска:
- **Домен:** irkutsk.bst-hab.ru
- **Счетчик Метрики:** отдельный для Иркутска
- **Целевые страницы:**
  - irkutsk.bst-hab.ru
  - irkutsk.bst-hab.ru/contacts
  - и т.д.

### ✅ Преимущества такого подхода:
1. Раздельная аналитика по городам
2. Разные рекламные кампании
3. Разные бюджеты
4. Точная атрибуция конверсий
5. Максимальный SEO для каждого города

---

## 📊 Формы обратной связи

Все формы автоматически добавляют город в письмо:

### Хабаровск (bst-hab.ru):
```
Город: Хабаровск
Имя: Иван
Телефон: +7 999 123-45-67
...
```

### Иркутск (irkutsk.bst-hab.ru):
```
Город: Иркутск
Имя: Петр
Телефон: +7 999 765-43-21
...
```

Это позволяет легко различать заявки из разных городов!

---

## ✅ Чеклист перед деплоем

- [ ] Запустили `npm run build`
- [ ] Проверили, что папка `/out` создана
- [ ] В `/out` есть `index.html`
- [ ] В `/out/irkutsk` есть `index.html`
- [ ] Загрузили главный сайт на bst-hab.ru
- [ ] Загрузили поддомен на irkutsk.bst-hab.ru
- [ ] Проверили редирект с bst-hab.ru/irkutsk → irkutsk.bst-hab.ru
- [ ] Настроили Яндекс.Метрику для обоих доменов
- [ ] Зарегистрировали оба домена в Яндекс.Вебмастер
- [ ] Проверили sitemap.xml на обоих доменах

---

## 🆘 Решение проблем

### Ошибка: "index.html not found"
**Решение:** Убедитесь, что загружаете содержимое `/out`, а не корень проекта

### Редирект не работает
**Решение:** 
1. Для статики редирект работает через JavaScript (клиентский)
2. Для серверной настройки используйте nginx/Apache конфигурацию выше

### Формы не отправляются
**Решение:** Проверьте `.env` файл - все переменные EmailJS должны быть заполнены

### Картинки не загружаются
**Решение:** Убедитесь, что папка `/out/img` загружена на хостинг

---

## 🎉 Готово!

После деплоя у вас будет:
- ✅ Два полноценных сайта с правильным SEO
- ✅ Раздельная аналитика
- ✅ Правильные редиректы
- ✅ Формы с различием по городам
- ✅ Максимальная оптимизация для Яндекс.Директ

**Сайты готовы к продакшену!** 🚀

