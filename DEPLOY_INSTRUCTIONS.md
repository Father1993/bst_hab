# 📦 Инструкция по деплою

## ⚠️ ВАЖНО: Два варианта деплоя

У вас настроено 2 города (Хабаровск и Иркутск). Есть два подхода:

---

## 🎯 Вариант 1: Статический хостинг (текущий)

### Что нужно сделать:

1. **Соберите проект:**
   ```bash
   npm run build
   ```
   
   Это создаст папку `/out` со всеми статическими файлами.

2. **Два отдельных деплоя:**

   ### Для главного сайта (bst-hab.ru):
   - Загрузите содержимое папки `/out` на хостинг
   - Корневой файл: `/out/index.html`
   
   ### Для поддомена (irkutsk.bst-hab.ru):
   - Создайте ОТДЕЛЬНЫЙ проект на хостинге
   - Загрузите содержимое папки `/out/irkutsk` на хостинг поддомена
   - Корневой файл: `/out/irkutsk/index.html`
   
   **ИЛИ** настройте nginx/веб-сервер для перенаправления:
   ```nginx
   # Для irkutsk.bst-hab.ru
   server {
       server_name irkutsk.bst-hab.ru;
       root /path/to/out/irkutsk;
       
       location / {
           try_files $uri $uri/ /index.html;
       }
   }
   ```

3. **Структура файлов в /out:**
   ```
   /out
   ├── index.html           # Главная Хабаровск
   ├── about/
   ├── contacts/
   ├── irkutsk/
   │   ├── index.html       # Главная Иркутск
   │   └── contacts/
   └── ...
   ```

### ❌ Ограничения:
- Нет middleware (поддомены работают только через конфигурацию веб-сервера)
- Нужно 2 отдельных деплоя ИЛИ настройка nginx

---

## 🚀 Вариант 2: Node.js хостинг с SSR (рекомендуется)

### Преимущества:
- ✅ Middleware работает
- ✅ Один деплой для обоих доменов
- ✅ Поддомены работают автоматически
- ✅ Серверные функции доступны

### Что нужно:
1. **Используйте Node.js хостинг** (не статический)
2. **Верните SSR конфигурацию:**
   
   В `next.config.js` удалите:
   ```js
   output: 'export',
   distDir: 'out',
   ```
   
   Верните `middleware.ts`

3. **Деплой:**
   ```bash
   npm run build
   npm run start
   ```

4. **На хостинге настройте:**
   - Start command: `npm run start`
   - Port: 3000
   - DNS: оба домена (bst-hab.ru и irkutsk.bst-hab.ru) указывают на один сервер

### Рекомендуемые платформы:
- **Vercel** (лучший для Next.js) - бесплатно
- **Railway** - до $5/мес
- **Render** - бесплатно
- **DigitalOcean App Platform**
- **VPS** с Node.js (ваш текущий хостинг, если поддерживает Node.js)

---

## 🎯 Что выбрать?

### Выбирайте **Вариант 1** (статический), если:
- ❌ У вас только статический хостинг (без Node.js)
- ✅ Готовы делать 2 отдельных деплоя
- ✅ ИЛИ умеете настраивать nginx/веб-сервер

### Выбирайте **Вариант 2** (SSR), если:
- ✅ Хостинг поддерживает Node.js
- ✅ Хотите один деплой для обоих доменов
- ✅ Нужны серверные функции в будущем

---

## 📝 Текущее состояние

Сейчас проект настроен на **Вариант 1** (статический экспорт).

Для деплоя:
```bash
npm run build
```

Затем загрузите содержимое папки `/out` на ваш хостинг.

---

## 🆘 Если ошибка "index.html not found"

Убедитесь, что:
1. ✅ В `next.config.js` есть `output: 'export'`
2. ✅ После `npm run build` создалась папка `/out`
3. ✅ В `/out` есть файл `index.html`
4. ✅ Вы загружаете содержимое `/out`, а не корень проекта
5. ✅ На хостинге корневая папка указывает на `/out`

---

## 💡 Рекомендация

Для **максимального SEO** и простоты - используйте **Вариант 2 (SSR на Vercel/Railway)**:

1. Зарегистрируйтесь на Vercel.com
2. Подключите GitHub репозиторий
3. Vercel автоматически задеплоит
4. Настройте DNS для обоих доменов
5. Готово! 🎉

Все будет работать автоматически с поддоменами.

